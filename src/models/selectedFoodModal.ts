export interface SelectedFood
{
    key? : string;
    // uid : string;
    name : string;
    calories : number;
    quantity : number;
    add_on_name? : string;
    add_on_quantity? : number;
    totalCalories : number;
    measure_in : string;
    // image? : string;
}